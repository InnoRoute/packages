#!/bin/bash

echo "Devices to be used:"
echo "* /dev/tpm0      # TPM (Infineon SLB9660 TPM 1.2)"
echo "* /dev/watchdog0 # Watchdog (TQ-Systems-specific implementation)"

echo "Side programs: command line history (might over time damage USB flash/SD flash), watch, cat, grep, vim, diff, od (-> coreutils), bwm-ng, ovs, netcat, sshd, tmux, lspci, lsusb, i2c-tools, ndpi, ..."
