#!/bin/bash

# Devices to be used:
#* /dev/tpm0      # TPM (Infineon SLB9660 TPM 1.2)
#* /dev/watchdog0 # Watchdog (TQ-Systems-specific)

# Side programs: command line history, watch, cat, grep, vim, diff, od (-> coreutils), bwm-ng, ovs, netcat, sshd, tmux, lspci, lsusb, i2c-tools, ndpi, ...
